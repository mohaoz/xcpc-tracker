[build]
command = "npm ci --prefix web && npm run catalog:refresh-xcpcio-board && npm run catalog:refresh-codeforces-award-cutoffs && npm run catalog:validate && npm run build --prefix web"
publish = "web/dist"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
