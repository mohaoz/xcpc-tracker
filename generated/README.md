# generated

Derived dataset artifacts for frontend consumption live here.

Examples:

- single generated catalog bundle JSON
- optional generated lookup tables for provider/problem mapping

These files should be reproducible from `catalog/` plus `scripts/`.

For the current transition, a small hand-written sample index may exist temporarily to make the target shape reviewable before generation scripts are implemented.
